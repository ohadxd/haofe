import { SlowScrollDirective } from './slow-scroll.directive';

describe('SlowScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SlowScrollDirective();
    expect(directive).toBeTruthy();
  });
});
